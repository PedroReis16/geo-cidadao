import { definePreset } from '@primeng/themes';
import Aura from '@primeng/themes/aura';

export const GeoCidadaoTheme = definePreset(Aura, {
  semantic: {
    colorScheme: {
      light: {
        primary: {},
      },
      dark: {
        primary: {},
      },
    },
  },
});
